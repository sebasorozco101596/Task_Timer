<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="www.sebasorozco.com.tasktimer" >

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskTimer"
        android:fullBackupContent="@xml/backup_descriptor"
        android:allowBackup="false"
        tools:ignore="AllowBackup">
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.TaskTimer.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <provider
            android:authorities="www.sebasorozco.com.tasktimer.provider"
            android:name="www.sebasorozco.com.tasktimer.data.database.AppProvider"
            android:exported="false"
            />
    </application>

</manifest>